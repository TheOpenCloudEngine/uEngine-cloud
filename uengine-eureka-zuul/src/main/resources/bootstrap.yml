spring:
  application:
      name: uengine-cloud
  profiles:
    active: "dev"

---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://config.pas-mini.io

---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://marathon-lb-internal.marathon.mesos:10000