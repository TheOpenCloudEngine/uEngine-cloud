management:
  security:
    enabled: false

security:
  basic:
    enabled: false
  ignored: /**

---
spring:
  security:
    enabled: false
  profiles: local

server:
  port: 8080
  servletPath: /

---
spring:
  security:
    enabled: false
  profiles: docker

server:
  port: 8080
  servletPath: /