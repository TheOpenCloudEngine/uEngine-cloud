spring:
  application:
      name: uengine-cloud-server
  profiles:
    active: "local"

---
spring:
  profiles: local
  cloud:
    config:
      uri: http://@host.config@

---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://marathon-lb-internal.marathon.mesos:10000